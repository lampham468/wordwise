{
  "name": "wordwise",
  "version": "1.0.0",
  "description": "AI-powered writing assistant for engaging messages",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "build": "npm run build --workspace=app",
    "preview": "npm run preview --workspace=app",
    "lint": "npm run lint --workspace=app",
    "type-check": "npm run type-check --workspace=app",
    "test": "npm run test --workspace=app"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": ["writing-assistant", "ai", "grammarly-clone", "text-editor"],
  "author": "",
  "license": "MIT"
}
